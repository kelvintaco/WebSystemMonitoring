@page "/"

<PageTitle>Home</PageTitle>

<div class="home-background">
    <div class="home-content">
        <h1 class="welcome-title">WELCOME</h1>
        <div class="button-row">
            <NavLink href="MasterList" class="home-btn">Master List</NavLink>
            <NavLink href="ItemList" class="home-btn">Item List</NavLink>
            <NavLink href="archiveList" class="home-btn">Archive List</NavLink>
            <NavLink href="printform" class="home-btn">Print Forms</NavLink>
        </div>
    </div>
</div>
<style>
    .home-background {
        min-height: 100vh;
        width: 100vw;
        background: linear-gradient(rgba(18, 42, 255, 0.85), rgba(134, 136, 243, 0.85)), url('/townhall.jpg') center center/cover no-repeat fixed;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0;
        padding: 0;
        position: absolute;
        top: 0;
        left: 0;
    }

    .home-content {
        text-align: center;
        width: 100%;
        padding: 2rem;
    }

    .welcome-title {
        font-size: 4rem;
        font-weight: 500;
        margin-bottom: 2.5rem;
        letter-spacing: 2px;
        color: #333;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
    }

    .button-row {
        display: flex;
        justify-content: center;
        gap: 2rem;
        flex-wrap: wrap;
    }

    .home-btn {
        background: #888;
        color: #fff;
        border: none;
        border-radius: 2rem;
        padding: 0.8rem 2.5rem;
        font-size: 1.1rem;
        font-weight: 500;
        cursor: pointer;
        transition: background 0.2s, transform 0.2s;
        box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        text-decoration: none;
        display: inline-block;
    }

        .home-btn:hover {
            background: #555;
            transform: translateY(-2px) scale(1.05);
            color: #fff;
            text-decoration: none;
        }

    media (max-width: 768px) {
        .button-row
    {
        flex-direction: column;
        gap: 1rem;
    }

    .welcome-title {
        font-size: 3rem;
    }

    }
</style>